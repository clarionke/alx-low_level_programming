#include "main.h"

/**
 * _strlen - writes the length of a string
 * @str: The string to get the length of
 *
 * Return: The length of @str.
 */
int _strlen(char *s)
{
	char length = 0;

	while (*str++)
		length++;

	return (length);
}
